'use strict';

var assert = require('assert');
//var gameOfLifeChamix = require('../lib');

describe('game-of-life-chamix', function () {
  it('should have unit test!', function () {
    assert.strictEqual(false, false);
  });
});
